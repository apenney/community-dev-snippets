Pull Request - Missing Tests

This looks good, but before we can merge the change set into the code base we also need to include examples that exercise this new behavior so we're able to validate the expected results.
