Pull Request - Failing Tests

While running the tests, I noticed the following failures.  Unfortunately, we're not able to merge this change set into the code base if the change causes other example behaviors to fail.
